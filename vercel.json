{
    "build": {
      "env": {
        "PKG_CONFIG_PATH": "/usr/lib/pkgconfig:/usr/share/pkgconfig",
        "LD_LIBRARY_PATH": "/usr/lib"
      },
      "extraInstallCommand": "apt-get update && apt-get install -y libcairo2-dev libjpeg-dev libpango1.0-dev libgif-dev build-essential g++"
    }
  }
  